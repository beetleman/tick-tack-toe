(ns tick-tack-toe.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [tick-tack-toe.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 1))))
